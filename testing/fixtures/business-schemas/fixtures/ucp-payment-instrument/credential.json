{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:schemaform:corpus:ucp-payment-instrument:credential",
  "$comment": "Adapted from the Apache-2.0 Universal Commerce Protocol Payment Credential schema; this file has been changed only to use an offline corpus identity and explicit open-object policy.",
  "title": "Payment Credential",
  "description": "The base definition for a payment credential.",
  "type": "object",
  "required": ["type"],
  "properties": {
    "type": { "type": "string" }
  },
  "additionalProperties": true
}
